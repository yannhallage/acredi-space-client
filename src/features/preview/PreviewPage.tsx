import { DesignCanvas } from '../../layouts/PreviewLayout';
import { PreviewLinks } from './components';

export function PreviewPage() {
  return (
    <DesignCanvas>
      <PreviewLinks />
    </DesignCanvas>
  );
}
